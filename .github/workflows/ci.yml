name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

      - name: Run tests
        run: npm test

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        run: echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker image
        run: |
          docker build -t ghcr.io/${{ github.repository }}:${{ github.sha }} .
          docker tag ghcr.io/${{ github.repository }}:${{ github.sha }} ghcr.io/${{ github.repository }}:latest

      - name: Push to GHCR
        run: |
          docker push ghcr.io/${{ github.repository }}:${{ github.sha }}
          docker push ghcr.io/${{ github.repository }}:latest

  deploy-staging:
    name: Deploy to Render Staging environment
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/staging'  # Alleen op staging branch

    steps:
      - name: Trigger Render Deploy
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_STAGING }}"
          echo "Deployment triggered for staging env!"
      
      - name: Wait for staging deployment
        run: |
          echo "Deployment started. Check Render dashboard for status."
          echo "URL: https://devops-app-ivan-staging.onrender.com"
      - name: Wait for staging deployment to be live
        run: |
          echo "Waiting for staging deployment..."
          sleep 120
          curl -f https://devops-app-ivan-staging.onrender.com/health || exit 1
          echo "Deployment to staging env successful!"        
  
  deploy-prod:
    name: Deploy to Render Production environment
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/main'  # Alleen op main branch

    steps:
      - name: Trigger Render Deploy
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
          echo "Deployment triggered!"
      
      - name: Wait for prod deployment
        run: |
          echo "Deployment started. Check Render dashboard for status."
          echo "URL: https://devops-app-ivan.onrender.com"
      - name: Wait for deployment to be live
        run: |
          echo "Waiting for prod deployment..."
          sleep 120
          curl -f https://devops-app-ivan.onrender.com/health || exit 1
          echo "Deployment prod env successful!"